import styled from "styled-components";
import { ColorsDesign } from "../../style";

export const StyledSelect = styled.div`
  width: 627px;
  height: 58px;
  border-radius: 8px;
  display: flex;
  justify-content: space-around;

  margin: 76px auto;
  border: 1px solid ${ColorsDesign.font};
`;
